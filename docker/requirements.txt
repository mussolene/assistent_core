# Runtime deps for assistant-core and telegram-adapter (synced with pyproject.toml).
# Regenerate after changing pyproject: pip install -e . && pip freeze | grep -E '^redis|^httpx|^openai|^pydantic|^pyyaml|^sentence' > docker/requirements.txt
redis>=5.0.0
httpx>=0.27.0
openai>=1.12.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
sentence-transformers>=2.2.0
