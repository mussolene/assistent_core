# Copy to .env and fill. Never commit .env.
# Used by docker-compose and local runs from project root.

REDIS_URL=redis://redis:6379/0
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321
OPENAI_BASE_URL=http://host.docker.internal:11434/v1
OPENAI_API_KEY=ollama
CLOUD_FALLBACK_ENABLED=false

# Git skill: clone/push require network; create_mr requires token
# SANDBOX_NETWORK_ENABLED=false
# GITHUB_TOKEN=
# GITLAB_TOKEN=
# Путь для клонирования репо (по умолчанию = SANDBOX_WORKSPACE_DIR). Для отдельного тома задайте, напр. /git_repos
# GIT_WORKSPACE_DIR=

# Telegram: URL дашборда в ответ на /settings и /channels
# DASHBOARD_URL=http://localhost:8080
# Chat ID для MCP/агента (уведомления, confirm). Для личных чатов = User ID
# TELEGRAM_DEV_CHAT_ID=
